<h1>All Songs</h1>

<% @songs.each do |song| %>
  <section id="song-<%= song.id %>">
    <h3><%= song.title %></h3>
    <p>Play count: <%= song.play_count %></p>
    <p>Length: <%= song.length %></p>
  </section>
<% end %>
